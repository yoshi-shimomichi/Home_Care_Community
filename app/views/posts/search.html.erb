<div class="container pt-3">
  <%= render 'search_form' %>
  <div class="row">
    <div class="col-12">
      <div class="row justify-content-center">      
        <% if @posts.present? %>
          <%= render @posts %>
        <% else %>
          <p>検索結果がありませんでした</p>
        <% end %>
      </div>
    </div>
  </div>
</div>